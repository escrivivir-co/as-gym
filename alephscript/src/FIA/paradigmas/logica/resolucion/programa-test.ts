import { UnificadorGeneral } from "../unificador/unificacion-general";
import { Parser } from "./parser";



/*
const declaraciones: IDeclaracion[] = [];


const pXY = new Declaracion({
    v: "p",
    terminos: [new dT("X", Tipo.V), new dT("Y", Tipo.V)]
});

const n1 = new Nodo(d1);

const qX: dTF = new Declaracion({
    v: "q",
    terminos: [new dT("X", Tipo.V)]
});

const rYZ = new Declaracion({
    v: "r",
    terminos: [new dT("Y", Tipo.V), new dT("Z", Tipo.V)]
});

const qY = new Declaracion({
    v: "q",
    terminos: [new dT("Y", Tipo.V)]
});

const qa = new Declaracion({
    v: "q",
    terminos: [new dT("a", Tipo.c)]
});

const rab = new Declaracion({
    v: "r",
    terminos: [new dT("a", Tipo.c), new dT("b", Tipo.c)]
});

const rbb = new Declaracion({
    v: "r",
    terminos: [new dT("b", Tipo.c), new dT("b", Tipo.c)]
});


*/